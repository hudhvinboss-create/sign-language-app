# Models

This directory is intentionally kept lightweight. The application downloads required MediaPipe model assets at runtime when they are missing.

Do not commit large generated model binaries here unless the project explicitly needs to redistribute them.
